import "twin.macro";
import Section, { SectionContent, SectionTitle } from "@/ui/Section";

const Bestuur = () => {
  return (
    <Section>
      <SectionTitle>Het bestuur</SectionTitle>
      <SectionContent></SectionContent>
    </Section>
  );
};

export default Bestuur;
